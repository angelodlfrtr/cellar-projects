<div id="new_session">
  <div class="container">
    <div class="columns">
      <div class="column col-4"></div>
      <div class="column col-4">
        <div class="card">
          <div class="card-header">
            <h4 class="card-title">Sign in</h4>
            <h5 class="card-subtitle">Sign in to Cellar projects management tool</h5>
          </div>

          <div class="card-body">
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

              <div class="form-group">
                <%= f.label :login, 'Email or username', class: 'form-label' %><br />
                <%= f.text_field :login, autofocus: true, class: 'form-input' %>
              </div>

              <div class="form-group">
                <%= f.label :password, class: 'form-label' %><br />
                <%= f.password_field :password, autocomplete: 'off', class: 'form-input' %>
              </div>

              <% if devise_mapping.rememberable? -%>
                <div class="form-group">
                  <label class="form-switch">
                    <%= f.check_box :remember_me %>
                    <i class="form-icon"></i> Remember me
                  </label>
                </div>
              <% end -%>

              <div class="form-group">
                <%= f.submit "Sign in", class: 'btn btn-primary' %>
              </div>
            <% end %>
          </div>

          <div class="card-footer">
            <%= render "devise/shared/links" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

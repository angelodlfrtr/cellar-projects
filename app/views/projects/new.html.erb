<div class="container">
  <div class="columns">
    <div class="column col-2 col-xl-3 col-md-12 col-sm-12 col-xs-12">
      <div class="card">
        <div class="card-header">
          <h4 class="card-title">Nouveau projet</h4>
        </div>

        <div class="card-body">
          Créez ici un nouveau projet. Vous pourrez y ajouter des utilisateurs,
          tâches, documents, ... une fois crée.
        </div>
      </div>
    </div>

    <div class="column col-6 col-xl-9 col-md-12 col-sm-12 col-xs-12">
      <div class="card">
        <div class="card-body">
          <%= form_for @project do |f| %>

            <div class="form-group">
              <%= f.label :name, 'Nom du projet', class: 'form-label' %>
              <%= f.text_field :name, class: 'form-input', autofocus: true %>
            </div>

            <div class="form-group">
              <%= f.label :description, 'Description', class: 'form-label' %>
              <%= f.text_area :description, class: 'form-input', rows: 5 %>
            </div>

            <div class="form-group">
              <%= f.label :deadline, 'Deadline', class: 'form-label' %>
              <%= f.date_select :deadline, class: 'form-input' %>
            </div>

            <div class="divider"></div>

            <div class="form-group text-right">
              <%= f.submit 'Créer le projet', class: 'btn btn-primary' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

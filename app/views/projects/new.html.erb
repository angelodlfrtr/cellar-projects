<div class="container">
  <div class="columns">
    <div class="column col-2 col-xl-3 col-md-12 col-sm-12 col-xs-12">
      <div class="card">
        <div class="card-header">
          <h4 class="card-title">New project</h4>
        </div>

        <div class="card-body">
          Here, create a new project. You will can add users, tasks, documents once created.
        </div>
      </div>
    </div>

    <div class="column col-6 col-xl-9 col-md-12 col-sm-12 col-xs-12">
      <div class="card">
        <div class="card-body">
          <%= form_for @project do |f| %>

            <div class="form-group">
              <%= f.label :name, 'Project name', class: 'form-label' %>
              <%= f.text_field :name, class: 'form-input', autofocus: true %>
            </div>

            <div class="form-group">
              <%= f.label :description, 'Project description', class: 'form-label' %>
              <%= f.text_area :description, class: 'form-input', rows: 5 %>
            </div>

            <div class="form-group">
              <%= f.label :deadline, 'Deadline', class: 'form-label' %>
              <%= f.date_select :deadline, class: 'form-input' %>
            </div>

            <div class="divider"></div>

            <div class="form-group text-right">
              <%= f.submit 'Create project', class: 'btn btn-primary' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
